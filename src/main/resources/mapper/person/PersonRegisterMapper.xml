<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.joyfishs.dawa.person.mapper.PersonRegisterMapper">

    <select id="queryList" resultType="com.joyfishs.dawa.person.entity.Person">
        select b.*
        from xm_person_register a
        left join xm_person b on a.person_id = b.id
        left join sys_org c on a.org_id = c.id
        where a.is_delete = 0
        and b.is_delete = 0
        <if test="orgId != null and orgId != ''">
            and (a.org_id = #{orgId} or c.pids like concat('%[', #{orgId} ,']%') )
        </if>
        order by a.create_time desc
    </select>

</mapper>
