<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.joyfishs.dawa.course.mapper.CoursewareMapper">

    <select id="findResourceList" resultType="com.joyfishs.dawa.course.domain.vo.CoursewareVo">
        SELECT *,a.video_path as fileUrl, a.if_resources as resources,
        (select name from sys_user where id = a.create_by) as publishName
        FROM xm_courseware a
        WHERE a.is_delete = 0 AND if_resources = 1
        <if test='type != null and type != ""'>
            AND a.type = #{type}
        </if>
        ORDER BY create_time DESC
    </select>
</mapper>
